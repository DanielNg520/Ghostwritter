# Historical/documentation only — real secrets now live in the sops/age
# encrypted config/secrets.enc.yaml (see config/secrets.example.yaml for its
# shape, and server/secrets_loader.py for how the server reads it). This
# file just documents the variable names for reference; nothing reads a
# real .env anymore.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
GROQ_API_KEY=
GROQ_MODEL=
